<%= form_with(model: sprint,:url => {:action => route_path}) do |form| %>
  <% if sprint.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sprint.errors.count, "error") %> prohibited this sprint from being saved:</h2>

      <ul>
        <% sprint.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
          <%= form.hidden_field :project_id %>
  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label "Expected Ending date", style: "display: block" %>
    <%= form.date_select :expected_end_date %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>

<br>

